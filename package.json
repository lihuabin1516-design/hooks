{
  "name": "ccpanes-hooks-tooling",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CC-Panes Hooks tooling layer for task context, project policy, and auditable hook enforcement.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "cli": "node dist/src/cli.js",
    "smoke": "node scripts/smoke.mjs",
    "verify": "npm run typecheck && npm test && npm run build && npm run smoke"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
